# BEGIN TAHOR SENDER RULES

if address :domain :is "from" "unwanted-vendor.example.com" {
    discard;
    stop;
}

# Marketing-only rules stay in the classifier so receipts are preserved.
# marketing-only.example.com

# END TAHOR SENDER RULES
